<nav class="bg-white shadow-md">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <div class="flex items-center">
        <%= link_to root_path, class: "text-2xl font-serif text-blue-900 font-bold" do %>
          QR<span class="text-amber-600">Gastro</span>
        <% end %>
      </div>

      <div class="hidden md:flex items-center space-x-8">
        <%= link_to "Home", root_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
        <%= link_to "Restaurants", restaurants_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
        <% if user_signed_in? %>
          <%= link_to "Dashboard", dashboard_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
          <%= link_to "QR Codes", qr_codes_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "bg-blue-900 text-white px-4 py-2 rounded hover:bg-amber-600 transition-colors" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
          <%= link_to "Register", new_user_registration_path, class: "bg-blue-900 text-white px-4 py-2 rounded hover:bg-amber-600 transition-colors" %>
        <% end %>
      </div>

      <div class="md:hidden">
        <button id="mobile-menu-button" class="text-gray-700">
          <i class="fas fa-bars text-xl"></i>
        </button>
      </div>
    </div>

    <div id="mobile-menu" class="hidden md:hidden pb-4">
      <div class="flex flex-col space-y-4">
        <%= link_to "Home", root_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
        <%= link_to "Restaurants", restaurants_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
        <% if user_signed_in? %>
          <%= link_to "Dashboard", dashboard_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
          <%= link_to "QR Codes", qr_codes_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "bg-blue-900 text-white px-4 py-2 rounded hover:bg-amber-600 transition-colors inline-block" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-gray-700 hover:text-amber-600 transition-colors" %>
          <%= link_to "Register", new_user_registration_path, class: "bg-blue-900 text-white px-4 py-2 rounded hover:bg-amber-600 transition-colors inline-block" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
